# Polaris workflow state
.polaris-setup-progress.json
.polaris/

# Dependencies
node_modules/

# Build output
.next/
.turbo/
dist/
build/
coverage/
*.tsbuildinfo

# Env / secrets
.env
.env.local
.env.*.local

# AIC provisioning outputs — keep the folder via .gitkeep only
config/payment/aic/outputs/**
!config/payment/aic/outputs/.gitkeep

# Logs — keep the folder via .gitkeep only
logs/**
!logs/.gitkeep
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Service manager PID files
scripts/pids/*.pid
scripts/pids/*.json

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

.playwright-mcp/
.claude/
.pnpm-store/

# Local Caddy TLS/PKI data dir (used to work around a root-owned default cert store)
.caddy-data/
