# ai-merchant-services

A TypeScript fullstack application providing AI-powered merchant services.

## Stack

- **Language:** TypeScript
- **Runtime:** Node.js
- **Project type:** Fullstack (frontend + backend)

<!-- Update this section as the stack is finalized: framework (Next.js, Express, NestJS, etc.), database, ORM, UI library, etc. -->

## Build & Development

<!-- Fill in as the project is set up. Example structure: -->

```bash
# Install dependencies
# npm install

# Run development server
# npm run dev

# Build for production
# npm run build
```

## Testing

<!-- Fill in test commands, frameworks, and structure when configured. Example: -->

```bash
# Run tests
# npm test

# Run tests with coverage
# npm run test:coverage
```

## Lint & Typecheck

```bash
# Lint
# npm run lint

# Typecheck
# npx tsc --noEmit
```

## Project Structure

<!-- Describe the top-level directory layout once scaffolded. Example: -->
<!-- 
- `src/` — application source
  - `app/` or `pages/` — frontend routes/pages
  - `api/` or `server/` — backend API routes
  - `lib/` — shared utilities
- `tests/` or `__tests__/` — test files
-->

## Conventions

<!-- Document naming conventions, module patterns, and code style decisions here as they are established. -->

- TypeScript strict mode is expected
- Prefer named exports over default exports

## Test Conventions

<!-- Fill in once the test setup is defined: framework (Jest, Vitest, etc.), file naming pattern, test location (co-located vs. separate directory), mocking approach. -->
