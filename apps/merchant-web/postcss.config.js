// PostCSS pipeline for `merchant-web`: Tailwind + autoprefixer. Same shape as
// the other Next.js apps in the workspace.

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
