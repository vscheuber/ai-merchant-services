// PostCSS pipeline for `payment-admin-web`: Tailwind + autoprefixer. Same
// shape as the other Next.js apps in the workspace.

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
