// PostCSS pipeline for `payment-user-web`: Tailwind + autoprefixer. Same
// shape as the other Next.js apps in the workspace.

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
